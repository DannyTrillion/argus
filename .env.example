# CoinMarketCap Pro API key (get one at https://pro.coinmarketcap.com). Never commit the real key.
CMC_API_KEY=
# Anthropic API key for the Claude agent. Never commit the real key.
ANTHROPIC_API_KEY=
# Optional: port for the web UI (default 3000)
PORT=3100
